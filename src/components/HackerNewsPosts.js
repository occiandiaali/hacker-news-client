import React from 'react';
import {
  ActivityIndicator,
  FlatList,
  StyleSheet,
  Text,
  View,
} from 'react-native';
import {Separator} from './Separator';

export default function HackerNewsPosts({posts}) {}

const styles = StyleSheet.create({});
